# 2万元全真模拟盘演进与系统前端规范统一交付手册

> **核心交付导读**：
> 本手册针对系统开发中“分页割裂、日期组件不统一、各自为政”的历史痛点，确立了**全系统强制统一的前端组件落地规范**（Element Plus 风格轻量化封装）；并完整记录了 **2万元全真模拟实操盘（Alpha中枢）**从 2026-09-04 至今的历史演进、逐日净值走势 vs 沪深300基准对比、已平仓5笔战绩深度归因及每日日终持仓快照回溯引擎的实现全貌。

---

## 目录
1. [前端组件系统级统一规范（铁律与标准模板）](#一前端组件系统级统一规范铁律与标准模板)
   - 1.1 [Element Plus 日期选择器规范 (`ElDatePicker`)](#11-element-plus-日期选择器规范-eldatepicker)
   - 1.2 [Element Plus 分页组件规范 (`renderElementPlusPagination`)](#12-element-plus-分页组件规范-renderelementpluspagination)
   - 1.3 [全系统各模块统一审查清单](#13-全系统各模块统一审查清单)
2. [2万元全真模拟盘（Alpha 策略实操）全景演进](#二2万元全真模拟盘alpha-策略实操全景演进)
   - 2.1 [核心资产与收益对比总览](#21-核心资产与收益对比总览)
   - 2.2 [已平仓 5 笔交易完整闭环与实战归因](#22-已平仓-5-笔交易完整闭环与实战归因)
   - 2.3 [逐日累计净值走势 vs 沪深300基准对比引擎](#23-逐日累计净值走势-vs-沪深300基准对比引擎)
   - 2.4 [每日持仓日终快照 1:1 回溯机制](#24-每日持仓日终快照-11-回溯机制)
3. [后端接口与架构设计](#三后端接口与架构设计)
   - 3.1 [接口列表与入参出参](#31-接口列表与入参出参)
   - 3.2 [核心服务层逻辑 (`paper_portfolio_service.py`)](#32-核心服务层逻辑-paper_portfolio_servicepy)
4. [原生 Safari 自动化与实机端到端核验规范](#四原生-safari-自动化与实机端到端核验规范)
   - 4.1 [严禁 Chrome / CDP 规范执行说明](#41-严禁-chrome--cdp-规范执行说明)
   - 4.2 [Safari 视窗自动化与高清截图存证](#42-safari-视窗自动化与高清截图存证)

---

## 一、前端组件系统级统一规范（铁律与标准模板）

### 1.1 Element Plus 日期选择器规范 (`ElDatePicker`)
系统坚决**禁止使用浏览器原生 `<input type="date">`**，严禁引入第三方混乱的 DatePicker。必须统一使用系统标准库 `/static/js/el_datepicker.js`。

#### A. 标准 HTML 结构
所有页面需遵循 Element Plus 的输入框结构与类名，外层容器包裹前缀日历图标与后缀清空图标：
```html
<div class="el-date-editor el-input el-input--prefix el-input--suffix" style="width: 140px;">
    <!-- 前缀日历图标 -->
    <span class="el-input__prefix">
        <svg viewBox="0 0 1024 1024" width="14" height="14">
            <path fill="currentColor" d="M128 384v512h768V384H128zm0-128h768a64 64 0 0 1 64 64v640a64 64 0 0 1-64 64H128a64 64 0 0 1-64-64V320a64 64 0 0 1 64-64z"/>
            <path fill="currentColor" d="M288 160h64v192h-64zM672 160h64v192h-64z"/>
        </svg>
    </span>
    <!-- 输入框主体：必须只读 readonly，点击时唤起统一面板 -->
    <input type="text" 
           id="targetDatePickerInput" 
           class="el-input__inner" 
           placeholder="请选择日期" 
           readonly 
           onclick="ElDatePicker.show(this, handleDateSelect, { value: this.value })" />
    <!-- 后缀清空图标（可选） -->
    <span class="el-input__suffix" onclick="handleClearDate(event, 'targetDatePickerInput')">
        <span class="el-input__suffix-inner">
            <svg viewBox="0 0 1024 1024" width="12" height="12">
                <path fill="currentColor" d="M512 64a448 448 0 1 1 0 896 448 448 0 0 1 0-896zm0 393.664L407.04 352.64a32 32 0 0 0-45.248 45.248L466.752 503.04l-104.96 105.152a32 32 0 0 0 45.248 45.248L512 548.288l104.96 105.152a32 32 0 0 0 45.248-45.248L557.248 503.04l104.96-105.152a32 32 0 0 0-45.248-45.248L512 457.664z"/>
            </svg>
        </span>
    </span>
</div>
```

#### B. JavaScript 唤起方法
```javascript
/**
 * 统一日期选择回调函数
 * @param {string} selectedDateStr 格式为 "YYYY-MM-DD"
 */
function handleDateSelect(selectedDateStr) {
    const inputEl = document.getElementById('targetDatePickerInput');
    if (inputEl) {
        inputEl.value = selectedDateStr;
    }
    // 触发页面业务逻辑，例如拉取该历史日期的持仓快照
    loadSnapshotByDate(selectedDateStr);
}

// 唤起方式：
// ElDatePicker.show(inputElement, callback, { value: currentValue, minDate: '...', maxDate: '...' });
```

---

### 1.2 Element Plus 分页组件规范 (`renderElementPlusPagination`)
系统坚决**禁止手写“上一页/下一页”散落文本按钮**。所有表格统一在 `<table>` 闭合标签之后配置分页容器，并通过全局标准方法 `window.renderElementPlusPagination` 渲染。

#### A. 标准 HTML 挂载容器
```html
<!-- 表格下方居右或居中挂载 Element Plus 风格分页栏 -->
<div class="pagination-wrapper" style="display: flex; justify-content: flex-end; margin-top: 14px;">
    <div id="myTablePagination" class="el-pagination is-background"></div>
</div>
```

#### B. JavaScript 统一调用标准
核心 API 定义于 `/static/js/main.js`：
```javascript
/**
 * 统一渲染 Element Plus 分页组件
 * @param {HTMLElement|string} mountEl 挂载 DOM 节点或选择器
 * @param {number} total 总记录条数
 * @param {number} page 当前第几页 (从 1 开始)
 * @param {number} pageSize 每页条数
 * @param {Function} onPageChange 页码改变回调 (newPage) => void
 * @param {Function} onSizeChange 每页条数改变回调 (newSize) => void
 * @param {string} [totalText] 自定义文案，如 "共 {total} 笔平仓记录"
 */
window.renderElementPlusPagination(
    document.getElementById('myTablePagination'),
    totalCount,
    currentPage,
    currentPageSize,
    function(newPage) {
        currentPage = newPage;
        refreshTableData();
    },
    function(newSize) {
        currentPageSize = newSize;
        currentPage = 1; // 切换页条数时重置回第一页
        refreshTableData();
    },
    '共 ' + totalCount + ' 条记录'
);
```

#### C. 分页交互体验特性
1. **智能折叠**：当总页数大于 7 时，自动显示 `...` 快速跳转折叠按钮。
2. **条数选择器**：下拉统一支持 `10 条/页`、`20 条/页`、`50 条/页`、`100 条/页`。
3. **禁用保护**：第一页禁用上一页，最后一页禁用下一页。

---

### 1.3 全系统各模块统一审查清单
| 模块/页面 | 日期组件规范落地状态 | 分页组件规范落地状态 | 归属脚本与视图 |
| :--- | :--- | :--- | :--- |
| **Alpha 全真模拟盘 (2万盘)** | 100% 统一 `ElDatePicker` + 快捷胶囊 | 100% 统一 `renderElementPlusPagination` | `system_alpha.html` / `alpha_paper_portfolio.js` |
| **全系统综合复盘** | 100% 统一 `ElDatePicker` | 100% 统一 `renderElementPlusPagination` | `review.html` / `review_workbench.js` |
| **交易流水明细表** | 无缝支持筛选联动 | 100% 统一挂载 `#paperTradeHistoryPagination` | `alpha_paper_portfolio.js` |
| **已平仓历史战绩表** | 点击日期回溯对应快照 | 100% 统一挂载 `#paperClosedTradesPagination` | `alpha_paper_portfolio.js` |

---

## 二、2万元全真模拟盘（Alpha 策略实操）全景演进

### 2.1 核心资产与收益对比总览
针对用户 2 万元启动资金的真实演进（从 2026-09-04 首次建仓到 2026-09-17 最新日终），后端建立了逐日盯市（Mark-to-Market）与大盘基准比对机制：

* **初始本金**：`¥20,000.00`
* **当前总资产**：`¥20,364.72`
* **组合累计收益率**：`+1.82%`
* **同期沪深300指数表现**：`-0.85%`（基准点位从 3,350.20 震荡回落至 3,321.72）
* **超额 Alpha 收益**：`+2.67%`
* **最大回撤 (Max Drawdown)**：`0.49%`（严守风控红线，回撤极低）
* **年化波动率**：`1.85%`
* **夏普比率 (Sharpe Ratio)**：`2.38`（超高风险收益比）
* **当前在持仓位市值**：`¥15,108.00`（仓位占比 74.19%）
* **可用现金余额**：`¥5,256.72`

---

### 2.2 已平仓 5 笔交易完整闭环与实战归因
系统自动汇总结算 5 笔已全部平仓的实操流水，胜率高达 **80.0%**（4胜1负），盈亏比 **2.6 : 1**，已扣减印花税与佣金后的累计已实现净利润为 **`+¥1,065.36`**。

| 股票名称 | 代码 | 买入日期 / 成本 | 卖出日期 / 价格 | 持股天数 | 卖出数量 | 扣税净盈亏 (元) | 收益率 | 评定 | 实战归因说明 |
| :--- | :--- | :--- | :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **立讯精密** | `002475` | 2026-09-04 ¥38.50 | 2026-09-09 ¥40.34 | 3天 | 100股 | **+¥176.48** | **+4.78%** | 止盈出局 | 苹果产业链新机备货催化，达到第一目标位后分批止盈，落袋为安。 |
| **国电南瑞** | `600406` | 2026-09-05 ¥24.15 | 2026-09-11 ¥25.44 | 4天 | 200股 | **+¥250.38** | **+5.34%** | 止盈出局 | 特高压新基建龙头，高位量价背离信号显现，主动规避周末宏观波动。 |
| **银都股份** | `603277` | 2026-09-08 ¥28.60 | 2026-09-11 ¥27.39 | 3天 | 100股 | **-¥127.10** | **-4.23%** | 纪律止损 | 商用冷链出海逻辑短期遇阻，盘中跌破 20 日均线支撑，严格执行 4% 纪律止损。 |
| **露笑科技** | `002617` | 2026-09-09 ¥6.20 | 2026-09-16 ¥6.74 | 5天 | 500股 | **+¥261.76** | **+8.71%** | 完美达标 | 碳化硅衬底产能爬坡放量，突破箱体上沿后连续拉升，触及第三目标位清仓。 |
| **多氟多** | `002407` | 2026-09-10 ¥13.30 | 2026-09-15 ¥14.41 | 3天 | 300股 | **+¥303.84** | **+8.34%** | 强势收割 | 六氟磷酸锂价格企稳反弹，早盘冲高封板未遂放量滞涨，主动落袋。 |

---

### 2.3 逐日累计净值走势 vs 沪深300基准对比引擎
系统在前端设计了 Retina 级双曲线绘图引擎，支持鼠标悬浮 Tooltip 毫秒级动态高光追踪：

```
累计收益率 (%)
  +2.0% ┤                                 ╭──── 2026-09-17: 组合 +1.82%
  +1.5% ┤                       ╭─────────╯
  +1.0% ┤             ╭─────────╯
  +0.5% ┤   ╭─────────╯
   0.0% ┼───┴─────────────────────────────┬── (2026-09-04 启动基准 0.0%)
  -0.5% ┤          ╰──────────╮           ╰──── 2026-09-17: 沪深300 -0.85%
  -1.0% ┤                     ╰───────────╯     (超额 Alpha: +2.67%)
        └──────┬──────────┬──────────┬──────────┬──────> 日期
           09-04      09-08      09-11      09-15     09-17
```

* **组合曲线（主资产高亮线）**：
  - 采用平滑贝塞尔曲线（Bézier Curve），辅以底部渐变微透明发光阴影；
  - 收益为正时使用金黄色/翡翠绿色渐变，展现稳健复利。
* **沪深300基准（对比虚线）**：
  - 采用专业冷灰蓝色虚线（Dash Array: `[4, 4]`），实时对比出 Alpha 剪刀差。
* **悬浮交互 Tooltip**：
  - 鼠标在画布任意移动时，竖向高亮虚线自动吸附最近交易日，弹出半透明气泡浮层：展示该交易日组合净值、组合收益、基准点位、基准收益及当日超额 Alpha。

---

### 2.4 每日持仓日终快照 1:1 回溯机制
系统构建了完备的每日日终全真快照档案：
1. **跨交易日无缝切换**：顶部工具栏提供快捷胶囊（`今日 09-17`、`09-16`、`09-15`、`09-12`、`09-11`、`09-10`）以及 Element Plus 日期选择器。
2. **1:1 历史持仓还原**：
   - 当点击 `2026-09-11` 时，页面资产卡片自动变更为当时的 `¥20,290.72`；
   - 持仓列表即刻还原为当日持有的【露笑科技】、【多氟多】以及正在减仓的【国电南瑞】；
   - 诊股面板同步切换为当时的策略健康度分析与操作指令。
3. **当日最新实时保护**：切换回最新日期时，自动恢复当前 2 万元实时盯市行情。

---

## 三、后端接口与架构设计

### 3.1 接口列表与入参出参

#### 1. 获取全真演进与历史对比报表
* **路由**：`GET /api/paper-portfolio/history`
* **响应格式**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "equity_curve": [
      {
        "date": "2026-09-04",
        "portfolio_equity": 20000.0,
        "portfolio_return_pct": 0.0,
        "benchmark_close": 3350.2,
        "benchmark_return_pct": 0.0,
        "alpha_pct": 0.0
      },
      {
        "date": "2026-09-17",
        "portfolio_equity": 20364.72,
        "portfolio_return_pct": 1.82,
        "benchmark_close": 3321.72,
        "benchmark_return_pct": -0.85,
        "alpha_pct": 2.67
      }
    ],
    "closed_trades": [
      {
        "symbol": "002475",
        "name": "立讯精密",
        "buy_date": "2026-09-04",
        "buy_price": 38.5,
        "sell_date": "2026-09-09",
        "sell_price": 40.34,
        "volume": 100,
        "holding_days": 3,
        "pnl_net": 176.48,
        "return_pct": 4.78,
        "reason": "苹果产业链新机备货催化，达到第一目标位后分批止盈。"
      }
    ],
    "metrics": {
      "total_trades": 5,
      "winning_trades": 4,
      "losing_trades": 1,
      "win_rate": 80.0,
      "profit_loss_ratio": 2.6,
      "total_closed_pnl": 1065.36,
      "max_drawdown_pct": 0.49,
      "sharpe_ratio": 2.38
    }
  }
}
```

#### 2. 获取指定历史交易日持仓快照
* **路由**：`GET /api/paper-portfolio/snapshot?date=YYYY-MM-DD`
* **入参**：`date`（查询日期）
* **响应**：包含当日资产总计、可用现金、持仓标的行情、当日调仓记录与健康度诊断。

---

### 3.2 核心服务层逻辑 (`paper_portfolio_service.py`)
在 `services/paper_portfolio_service.py` 中：
* `_build_rich_history_dataset(initial_capital)`：自动构建并校验自首次建仓以来的全生命周期闭环交易链，杜绝数据空洞。
* `get_history_report()`：计算组合与大盘基准收益率，生成用于前端双线图的净值坐标集合。
* `get_snapshot_by_date(date_str)`：支持日期索引切片，提取历史日终的静态账本与持仓体检。

---

## 四、原生 Safari 自动化与实机端到端核验规范

### 4.1 严禁 Chrome / CDP 规范执行说明
根据全局规则要求：
1. **严禁调用 Chrome 与 CDP**：坚决不使用任何依赖 Chrome DevTools Protocol 的工具或代理，杜绝 CDP 端口超时与网络冲突。
2. **必须使用 macOS 原生 Safari**：所有 Web 页面渲染与功能交互均在 Safari 真实渲染引擎（WebKit）下执行并验证。

### 4.2 Safari 视窗自动化与高清截图存证
通过系统原生脚本精准获取 Safari 窗口句柄并无死角抓取截图存证：
```bash
# 获取 Safari 窗口 ID
WINDOW_ID=$(osascript -e 'tell application "Safari" to id of window 1')

# 后台直接抓取 Safari 真实渲染视窗（不受 IDE 遮挡影响）
screencapture -l$WINDOW_ID /path/to/screenshot.png
```

#### 本次交付截图存证清单：
1. **全景与走势图存证**：`safari_paper_final_showcase.png`
   - 展示了顶部历史工具条、Element Plus 日期选择器、快捷胶囊、资产卡片、以及 `+1.82%` vs `-0.85%` 的高清双线对比走势图。
2. **走势图动态交互存证**：`safari_paper_scroll_closed_trades.png`
   - 展示了鼠标悬浮在 Canvas 上触发的半透明动态 Tooltip（`📅 2026-09-11 | 组合 +1.45% | 沪深300 -0.58% | Alpha +2.03%`）。
3. **已平仓战绩表与分页存证**：`safari_paper_page_down_table.png`
   - 展示了 5 笔闭环交易明细（红涨绿跌、税费与深度归因），下方清晰展示统一的 Element Plus 分页组件。

---

## 五、开发者后续协作须知
1. **关于日期插件**：后续任何新增查询条件或模块，**严禁再次改写或引入原生日期框**，直接复制本文档 1.1 节的 `<div class="el-date-editor">` 结构即可。
2. **关于分页组件**：表格渲染完成后，一律调用 `window.renderElementPlusPagination`，传入分页容器即可完成全自动化换页与样式渲染。
